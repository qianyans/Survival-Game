<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.survivalgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".beforeactivity.BeforeDodgeActivity"></activity>
        <activity android:name=".beforeactivity.BeforeReplayActivity" />
        <activity android:name=".uploadactivity.model.UploadActivity" />
        <activity android:name=".beforeactivity.BeforePongActivity" />
        <activity android:name=".beforeactivity.BeforeRunningActivity" />
        <activity android:name=".dodgegame.view.DodgeGameActivity" />
        <activity android:name=".runninggame.view.RunningGameActivity" />
        <activity android:name=".ponggame.view.PongGameActivity" />
        <activity android:name=".replay.view.PongGameReplayActivity" />
        <activity android:name=".scoreboard.view.RankingActivity" />
        <activity android:name=".loginsystem.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>